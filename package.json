{
  "private": true,
  "name": "whatlang",
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  },
  "packageManager": "yarn@4.2.2",
  "scripts": {
    "build": "esbuild whatlang_interpreter.ts --format=esm --bundle --minify --sourcemap  --outfile=whatlang_interpreter.js"
  }
}
